%table.table.table-hover
  %thead
    %tr
      %th Card
      %th Expansion
      %th
      %th
  %tbody
    - cards.each do |card|
      %tr
        %td= link_to card.name, mtg_db_card_path(card.id)
        %td= link_to card.cardSetId, ""
