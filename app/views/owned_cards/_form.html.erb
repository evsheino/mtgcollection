<%= form_for(@owned_card) do |f| %>
  <% if @owned_card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@owned_card.errors.count, "error") %> prohibited this owned_card from being saved:</h2>

      <ul>
      <% @owned_card.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Card %><br>
    <%= f.select :expansion_card_id, options_from_collection_for_select(@cards, :id, :to_s) %>
  </div>
  <div class="field">
    <%= f.label :number %><br>
    <%= f.number_field :number %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
